{
  "name": "tp-mongo-nodejs-crud",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker-node": "docker-compose up -d api",
    "docker-node-db": "docker-compose up -d",
    "run-local": "docker exec -it api /bin/bash -c 'API_ENV=dev node ./api'",
    "run-remote": "docker exec -it api /bin/bash -c 'API_ENV=prod node ./api'",
    "tests-local": "docker exec -it api /bin/bash -c 'API_ENV=dev  npx jest --detectOpenHandles --forceExit'",
    "tests-remote": "docker exec -it api /bin/bash -c 'API_ENV=prod  npx jest --detectOpenHandles --forceExit'"
  },
  "author": "Sercan YILDIZ <ysercanz@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.0"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "supertest": "^4.0.2"
  }
}
